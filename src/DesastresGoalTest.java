import aima.search.framework.GoalTest;

public class DesastresGoalTest implements GoalTest {
    // No usado para esta practica
    public boolean isGoalState(Object state) {
        return Boolean.FALSE;
    }
}
